# React + Node + Express + MongoDB #

### Global installation ###

* Nodemon (npm install -g nodemon)
* TypeScript Node (npm install -g ts-node)

* * *
### Development ###
frontend:    
`cd frontend`   
`yarn install`    
`yarn start`   

server:   
`cd backend`   
`yarn install`   
`yarn start`
